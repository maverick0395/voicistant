# Copy to /opt/voicistant/.env on the VPS. Never commit .env.
SITE_ADDRESS=              # <ip-with-dashes>.sslip.io, e.g. 65-21-10-42.sslip.io
MODEL_API_KEY=             # same key as in the vast template (vLLM + speaches)
VAST_API_KEY=              # scoped key: this instance only (SETUP.md §3)
VAST_INSTANCE_ID=
SESSION_SECRET=            # openssl rand -hex 32
SEARXNG_SECRET=            # openssl rand -hex 32
# IMAGE_TAG=<commit sha>   # rollback: pin an older image, then run deploy/deploy.sh
